package org.buaa.nlp.cj.jobOnlineProgram.hulu;

import java.util.Scanner;

/**
 * Created by Administrator on 2015/9/26.
 */
public class Knight {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        String line = scanner.nextLine();
        long n = Long.valueOf(line.trim().split(" ")[0]);
        long m = Long.valueOf(line.trim().split(" ")[1]);
        long l = Long.valueOf(line.trim().split(" ")[2]);
        long k = Long.valueOf(line.trim().split(" ")[3]);

        while (l > 0) {
//            String l = scanner.nextLine();
//            long dx = Long.valueOf(l.trim().split());
////            l--;
        }
    }

}
